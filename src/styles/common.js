import { StyleSheet } from 'react-native'

import { BLACK_COLOR } from './stylesConstants'

export default StyleSheet.create({
    max: {
        width: '100%',
        height: '100%'
    },
    blackBackground: {
        backgroundColor: BLACK_COLOR
    },
    centerX: {
        display: 'flex',
        alignItems: 'center'
    }
})